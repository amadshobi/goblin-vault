#!/usr/bin/env bash
# Inject NODE_PATH ke lokasi node_modules ~/.opencode agar binary tidak crash kehilangan dependensi
export NODE_PATH="$HOME/.opencode/node_modules"
exec node "$(dirname "$0")/../src/ocm/index.js" "$@"
